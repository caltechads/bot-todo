# Domain Docs

This repository uses one domain context documented in `CONTEXT.md` at the repository root. Read that glossary before planning or changing domain behavior.

Architecture decisions that meet the ADR threshold live under `docs/adr/`. If a referenced domain document does not exist, proceed without creating placeholders; Domain Modeling creates documents only when language or decisions are resolved.
